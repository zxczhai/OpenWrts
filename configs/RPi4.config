# Target Config
CONFIG_TARGET_bcm27xx=y
CONFIG_TARGET_bcm27xx_bcm2711=y
CONFIG_TARGET_bcm27xx_bcm2711_DEVICE_rpi-4=y

# =================================================================
# Custom Configuration for Netgear A6210 (mt7612u) USB Wi-Fi Adapter
# =================================================================

# 1. Main driver for MT7612U chipset
# 适用于 MT7612U 芯片的核心驱动
CONFIG_PACKAGE_kmod-mt76x2u=y

# 2. USB 3.0 support
# 确保 USB 3.0 驱动被启用 (对于树莓派4的配置，这通常是默认启用的，但显式写上更保险)
CONFIG_PACKAGE_kmod-usb3=y

# 3. Full WPA Supplicant for Wi-Fi Functionality
# (Recommended) 安装功能更全的 wpad，以支持 WPA3、802.11r 快速漫游等高级功能。
# 这会替换掉默认的基础版 wpad-basic-wolfssl。
# 如果默认配置中已经选择了 wpad-basic-wolfssl，需要先取消它。
# CONFIG_PACKAGE_wpad-basic-wolfssl is not set
CONFIG_PACKAGE_wpad-openssl=y
